func main()
{
    bool a = (true or false) and true;
    expect(0, a, true);

    bool b = (false or false) and true;
    expect(1, b, false);
}

func expect(int index, bool actual, bool expected)
{
    print("tc: " + index + ", actual: " + actual + ", expected: " + expected);
}