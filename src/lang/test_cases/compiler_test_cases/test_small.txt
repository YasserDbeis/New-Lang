func main()
{
    ~if (true)   offset -> elsif
    ~{

    ~    jmp -> A
    ~}
    ~elsif (true)   offset -> else
    ~{
    ~    jmp -> A
    ~}
    ~else        offset -> 1
    ~{

    ~}

    ~A
}
