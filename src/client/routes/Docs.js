import React from 'react';
import { useState } from 'react';

const Docs = (props) => {
  return (
    <main style={{ padding: '1rem 0' }}>
      <h2>Docs</h2>
    </main>
  );
};

export default Docs;
